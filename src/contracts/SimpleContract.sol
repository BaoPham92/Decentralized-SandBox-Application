pragma solidity ^0.5.2;

contract SimpleContract {
    string public value = "defaultValue";
    
    function set(string memory _value) public {
        value = _value;
    }

    // Transaction counts (standard) object.
        // Sign and serialize
        // Broadcast contract

    // Token transaction struct
}